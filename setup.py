from setuptools import setup, find_packages

setup(
    name="calendar_utils",
    version="0.1",
    #packages=find_packages(),
    install_requires=[],
    description="A simple calendar utility package",
)
